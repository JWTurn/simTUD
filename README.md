After knitting and rendering `simTUD.Rmd`, overwrite this file with a symlink to `simTUD.md`.

E.g., on Linux/macOS from terminal:

```bash
cd C:/Users/julie/Documents/simTUD
rm README.md && ln -s simTUD.md README.md
```
